class Vehicle {
    constructor(id, plate, renavam, model, brand, year) {
      this.id = id;
      this.plate = plate;
      this.renavam = renavam;
      this.model = model;
      this.brand = brand;
      this.year = year;
    }
}